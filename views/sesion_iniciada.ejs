<!-- index.html -->
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>FERREMAS - Sesión Iniciada</title>
<style>
  .modal {
    display: none;
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    padding: 20px;
    background-color: #f0f9eb;
    border: 2px solid #4caf50;
    border-radius: 10px;
    text-align: center;
  }
  .checkmark {
    color: #4caf50;
    font-size: 48px;
  }
</style>
</head>
<body>
<div id="myModal" class="modal">
  <div class="checkmark">✓</div>
  <p>Sesión Iniciada, Serás redirigido en 3 segundos</p>
</div>

<script>
  const modal = document.getElementById('myModal');
  modal.style.display = 'block';

  const userData = {
    nombre: "<%= users[0].NOMBRECITO %>",
    aidi: "<%= users[0].NUMERITO %>"
  };

  localStorage.setItem('userData', JSON.stringify(userData));
  localStorage.setItem('isLogged', 'true');

  // Redirect after 3 seconds
  setTimeout(() => {
    // Replace the URL with the desired redirection destination
    window.location.replace('/');
  }, 100);
</script>
</body>
</html>